# DECISIONS — 决策记录

> 每个决策：日期 + 决策 + 理由 + 被否决的选项。按时间倒序排列。

<!-- 模板：
## [日期] [决策标题]
- 决策: ...
- 理由: ...
- 被否决的选项: ...
- 备注: ...
-->

## [2026-08-15] 新增「食材咨询」入口 + 通用食材知识落点（references/ingredient_knowledge.md）

- 决策: 在 Meal Planning 下新增第三个独立功能「🥗 食材咨询」——用户点名某类食材问「怎么吃/怎么搭」时，读 `references/ingredient_knowledge.md`（通用食材知识）× `profile.json`（个性化画像）→ 输出「通用 × 个性化」结合体。知识文件按食材类分组，每组三件套 = ① 特性（客观事实）② 提味原理（通用方法）③ 安全建议（基于客观事实的可执行饮食建议）。食材咨询定位为只读（无确认闸门、不写数据）。
- 理由: 用户问「海带/木耳/银耳/魔芋这类无味水溶性纤维食物怎么搭」，答案是「通用知识 × 个性化需求」两层结合体——通用层（怎么提味、有什么注意）谁问都一样，个性化层（控糖护肝控LDL、凉拌煮蒸烙、常备洋葱酸奶）是"我"独有。现有两个入口（采购计划=买什么、每日搭配=今天吃什么）都不覆盖这个"点名某类食材"的咨询场景；且通用食材知识（提味原理、安全建议）此前只活在 agent 脑内、无落点。范围小——个性化层 100% 复用 profile.json，不改 schema，唯一新增是通用知识落点。
- 被否决的选项:
  - 把「典型搭法/固定食谱」（如"海带凉拌""木耳醋拌"）写进知识文件 —— 否决：那是每日搭配生成器的活，写死会与库存/画像冲突且僵化
  - 三件套第三项用「安全红线」（客观事实罗列）—— 否决：只描述事实不可执行，改为「安全建议」（基于事实给饮食建议，如"木耳冷藏泡、现泡现吃"而非"木耳有米酵菌酸风险"）
  - 作为每日搭配的变体而非独立入口 —— 否决：触发（点名某类食材）与输出（认识+提味+注意，非"今天三顿组合"）都不同，独立更清晰；但仍共享 profile 个性化层，不另起机制
- 备注: commit f8e9455（知识文件）+ b1557d4（SKILL.md 入口）；IDEAS.md「食材咨询入口 + 通用食材知识落点」→ implemented；static validation PASS

## [2026-08-13] 硬规则 #2 从「No code」放宽为「No-code core, optional scripts」（渐进增强）

- 决策: 推翻 AGENTS.md 硬规则 #2「No code, no scripts」，改为「No-code core, optional scripts」——skill 契约（SKILL.md + references）保持纯 prompt/schema，任何 agent 零代码执行即可用；新增可选 `scripts/` 目录承载确定性增强（JSON 完整性校验、数量校验算术、feedback 去重/幂等），有 shell 的 agent 执行拿更强保证，无 shell 的 agent 退回纯指令（优雅降级）。脚本永不替代 LLM 判断（搭配生成、健康建议）。
- 理由: 原「No code」一刀切是自我加严——Open Agent Skills / Claude Code 官方规范本身允许 skill 捆绑 scripts/（agent 经 bash 执行，`$SKILL_PATH/scripts/default.sh` 为标准惯例）。加严同时伤两块：(a) 开发期可测试性（无法自动化回归）；(b) 运行期可靠性（LLM 手写 JSON 缺逗号、算术出错、查重遗漏——恰是确定性环节，脚本最擅长）。放宽 = 回归规范，非背叛。核心定位：脚本是「增强层」非「依赖」——核心能力 100% no-code 守住跨 agent 底线。
- 被否决的选项:
  - 保持「No code」原样 —— 否决：可测试性与运行期可靠性持续缺失，且比规范更严、无必要
  - 整体改「low code」（脚本成为 skill 能力一部分）—— 否决：暗示部分能力依赖代码，破坏跨 agent 底线；正确定位是 no-code 核心 + 可选增强
  - 脚本塞进 references/ —— 否决：references/ 是给 agent 读的文档，scripts/ 是给 agent 执行的代码，语义与官方惯例应分离
- 备注: 两个正交层次——dev/（开发者验证，不进分发）vs scripts/（运行时增强，进分发），职责不同勿混。配套落点：AGENTS.md 硬规则 #2 + Repository Layout 补 scripts/。运行期脚本清单（validate_json.py / quantity_check.py / dedup.py）与开发期验证（validate_static.py / golden cases）随后续 step 落地。

## [2026-08-11] 每日搭配做法原则：组合级极简分组式（最少方法种类）
- 决策: 做法生成从"每食材一动作"改为**组合级极简分组式**——以整个食材组合为单位设计做法，优先用最少方法种类覆盖全部食材（能一种就一种：全部焯水后亚麻籽油拌 / 全部蒸后撒调料）；一种不够才按处理需求分组（能生吃的不焯水，需熟的才焯水/蒸/煮/烙），每组一句（"X、Y 焯水拌油，Z 生切"）。目标：方法种类最少、烹饪强度最低，最大限度保留原始营养与风味
- 理由: 2026-08-11 试用暴露"香菇煮汤"式失真输出——为凑排比句式给单味干香菇配复合做法，根因是做法规则只约束句式（1-2 short phrases）与健康性，缺组合级设计与可执行性约束。用户给出更本质的原则：极简烹饪——同一种做法处理尽可能多的食材（鸡胸肉+胡萝卜+洋葱+香菇 → 全部焯水后油拌 / 蒸后撒料），做法数量由食材处理需求决定（秋葵+生菜+黄瓜+苹果 → 秋葵焯水、其余生吃）
- 被否决的选项:
  - 增加"做法覆盖每食材 + 干货泡发 + 复合形式需≥2食材"式校验清单 —— 否决：为修而修、规则堆叠，违背极简目标（用户批评后撤回）
  - 维持现状（每食材一动作排比）—— 否决：句式优先于事实，产出不可执行做法
- 备注: SKILL.md Daily Pairings Rules 段 + 通用准则层 ③ 同步改写；feedback fb_w9fi4e → applied

## [2026-08-09] Feedback flow 独立为 references/feedback_flow.md（实施三层 hooks）
- 决策: 将 SKILL.md 反馈章节的完整状态机迁移至独立文件 `references/feedback_flow.md`，按三层 hooks（capture / threshold / review）组织；SKILL.md 只保留概述 + 原则声明（用户数据 vs SKILL.md 分工）+ 三处 citation（反馈章节、采购计划 PLAN-TIME REVIEW、每日搭配流程）；schema.md 互链 feedback_flow.md
- 理由: feedback flow 是 skill 运行重点，散文嵌正文导致每次 skill 加载都携带完整状态机、且正文越长越易被浏览略过；独立后按需加载（写侧捕获/落点时、读侧生成计划/搭配前），与 quantity_benchmark.md 的引用模式同构。SKILL.md 净减 ~87 行
- 被否决的选项: 无——本决策是既定想法的实施（IDEAS.md 条目），术语选型已在同日「三层 hooks」决策中完成
- 备注: commit 60e34ae；IDEAS.md「Feedback flow 独立成 reference 文件」→ implemented；三层 hooks 术语落地（capture hook / threshold hook / review hook）

## [2026-08-09] feedback flow 触发机制术语：三层 hooks
- 决策: feedback flow 的反射触发机制统一命名为**三层 hooks**——Layer 1 即时捕获 = `capture hook`、Layer 2 阈值整理 = `threshold hook`、Layer 3 计划前兜底 = `review hook`；术语进入 SKILL.md 反馈章节与（未来独立的）references/feedback_flow.md
- 理由: agent 无常驻进程——不存在"后台自己找活干"的守护进程，一切整理只能挂在宿主流程的自然执行点上（对话处理 / 日界 / 计划生成）；hooks 恰好同时编码"何时触发"与"挂在哪里"，比 triggers 多表达宿主-挂接关系。Layer 2 的阈值触发有文献出处：Generative Agents（Park 2023）重要性分数累积触发反思、MemGPT memory pressure 触发上下文整理
- 被否决的选项:
  - daemon（守护进程）—— 否决：agent 无常驻进程，无"主动监控"语义，时机由宿主决定而非自身
  - callback（回调）—— 否决：只覆盖即时触发（Layer 1），无法表达阈值批处理与消费时兜底
  - trigger policy / trigger points —— 否决：只表达"会触发"，不表达挂接位置与宿主关系，信息量不足
  - opportunistic reflection（机会式反思）—— 否决为术语，保留为语义注解：它准确描述了三层 hooks 的设计哲学（利用自然触点、不打扰用户），适合作为 hooks 的说明性描述而非正式命名
- 备注: 与 IDEAS.md「Feedback flow 独立成 reference」条目配套；实施 feedback_flow.md 迁移时统一采用三层 hooks 术语

## [2026-08-05] RQ-5 反馈机制设计：feedback.json + 三层触发 + 三级复用
- 决策: 用户建议与反馈的沉淀机制整体设计定稿——① 独立 `feedback.json`（4 类反馈 × importance 1-5，四杠杆整理：merge/decay/eviction + salience floor≥4）；② 三层触发（即时捕获：对话信号即记录、imp≥3 立即落点；阈值反思：当日累积 ≥8 或同实体 ≥3 条 merge 或同类型 ≥3 条升级；计划时回顾：仅当天有 feedback 时补漏落点）；③ 概念消解为「流程规则 / 画像偏好 / 偏好模板」三件套；④ 复用 = 生成时三级引用（模板层 > 画像层 > 通用准则层）+ 模板轮换 + 耗尽候选机制（与库存感知配对）
- 理由: 2026-08-05 实况演练证明即兴处理无保证（洋葱/酸奶靠追问才入库）。调研 4 类方案（Mem0 事实提取 / Hindsight 四杠杆 / GA 阈值反思 / Reflexion 失败触发）后选型 ②③④ 轻量组合，避开 ① 自动全量提取。约束：No-code 纯指令、跨 agent 通用、最小打扰、数据落点收敛
- 被否决的选项:
  - 引入 Mem0/Letta 等记忆框架 —— 否决：违反 AGENTS.md「No code, no scripts」硬规则，且依赖特定执行环境
  - feedback 并入 profile.json —— 否决：profile 是已收敛画像，feedback 是原始日志+沉淀轨迹，职责分离（用户决策，2026-08-05）
  - 新增第 5 类 organization-feedback —— 否决：存储/归置习惯归 preference-correction，content 注明即可（用户决策）
  - 保留"搭配规则"独立概念 —— 否决：与偏好模板重叠，按来源+生命周期消解为三件套（用户决策）
  - 自动全量事实提取（Mem0 式，异步无差别抽取）—— 否决：噪音大、违背最小打扰
  - 触发阈值设 ≥6 或 ≥10 —— 否决：≥8 为灵敏度与克制的中位（用户决策）
  - 第三层回顾每次计划都跑 —— 否决：仅当天有 feedback 时跑，省 token（用户决策）
- 备注: 设计底稿 RESEARCH.md（RQ-5 条目）；已实施（commit 2b91c88..6e6da4d）：SKILL.md Feedback 章节 + 采购计划/每日搭配接入 + schema.md（feedback.json/pairingTemplates/rules）+ README + 用户数据初始化

## [2026-08-05] 保留 Scheduled Reminders 中的 `cron add` 作为占位示例
- 决策: SKILL.md 底部 Scheduled Reminders 章节的 `cron add` 命令（Hermes 专属语法）不改动，保留为**占位示例**，表示该 skill 支持定时提醒能力（购物清单提醒/过期提醒/低库存提醒）
- 理由: 用户明确决定（2026-08-05）——该处仅占位示意"支持提醒"这一能力，不是可执行指令；各 agent 用自己的定时机制实现同样语义，跨 agent 通用性由语义保证，不因示例命令形式受影响
- 被否决的选项:
  - 清理为 agent 无关描述（如"用你平台的定时机制"）—— 否决：占位已足够表达能力，改动收益低
  - 删除整个提醒章节 —— 否决：会丢失"支持提醒"的能力声明
- 备注: AGENTS.md 验证清单对 `cron add` 的检查项在本章节为已知例外，无需改动 AGENTS.md

## [2026-08-05] 采购计划数量校验：基准表 + 确认闸门前校验步骤
- 决策: 🛒 采购计划在「推荐分量」与「确认闸门」之间新增数量校验步骤：agent 读取 references/quantity_benchmark.md（成人每日参考量/单件克重/品类归属），按 每日区间 × 段天数 × household.persons（默认 1）计算各品类应采区间，与提议量对比输出 ✅/⚠️/❌ 报告并给出收敛建议；只软提示不硬拦。profile.json 新增 household.persons 字段
- 理由: 真实试用复算暴露——清单总量超单人 3-4 天需求 1.5-2 倍（蔬菜/水果/肉鱼约 2 倍、薯类缺失），根因是分量无基准且确认闸门只查品种不查数量；按基准修正后复算全部达标，问题消除
- 被否决的选项:
  - 引入 scripts/ 校验脚本（Python）—— 否决：违反 AGENTS.md「No code, no scripts」硬规则；跨 agent 通用不能依赖执行环境；开发期 Python 分析仅用于定位问题，产出沉淀为基准表
  - 维持现状（不校验）—— 否决：数量问题不会自动暴露，确认闸门查品种不查数量
  - 硬拦截（超量即拒绝生成/自动收敛）—— 否决：数量受人数/食量/库存影响，agent 估算有误差，决定权应留在确认闸门
- 备注: commit c66baab；基准表含免责声明；生鲜从严（超量=浪费）、耐放从宽（多买不浪费）

## [2026-08-05] 规划流程重构：采购计划/每日搭配拆为独立功能，周计划降为编排器
- 决策: Weekly Plan 从「一次输出全周清单 + 7 天搭配并自动写入」重构为三个模块：🛒 采购计划（独立功能，含确认闸门——展示→用户确认/修改→才写入 shopping.json）、🍽 每日搭配（独立功能，食材池 = 已确认清单 ∪ 库存，按天分块、早/午/晚列表显示，支持单餐局部调整与清单联动重生成）、📆 周计划（编排器，按 shoppingRhythm 切段，逐段调用前两者并逐段确认，不整周倾倒）
- 理由: 真实试用暴露 4 个问题：①搭配必须派生自已确认清单（素材→组合依赖），清单先行 ②一次输出整周信息量过大，3-4 天采购对应 7 天搭配错位，且清单写入前无确认机会 ③搭配同样需要用户参与，早/午/晚应列表化显示 ④功能耦合使日常高频场景（"今晚吃什么"）无法独立触发
- 被否决的选项:
  - 维持现状（一次生成整周清单+搭配并自动写入）—— 否决：信息量过大、无确认闸门、节奏错位
  - 仅加确认步骤不拆功能 —— 否决：日常高频场景仍无法独立触发，耦合问题不解决
  - 先搭配后清单 —— 否决：搭配会引用未购买食材，素材→组合依赖决定必须清单先行
  - shopping.json 增加 segment/trip 字段区分段落 —— 否决：unchecked 项即当前段落待购项，段落由对话上下文承载，schema 无需变更
- 备注: commit aaf3813；IDEAS.md「规划流程重构」条目；第 5 项（用户反馈沉淀机制）留作研究问题

## [2026-08-05] 库存冷启动探测提升为通用规则（任意库存读取触发 + 一次性标记）
- 决策: 把 long-cycle 冷启动探测从 Weekly Plan 专属流程提升为通用规则：任何读取 pantry.json 且发现无长周期项的操作（重点是 View inventory）都触发一次友好开场；用 `meta.longCycleProbed` 标记保证每个数据文件只探测一次
- 理由: 试用发现「查看空库存」比「生成周计划」更早、更自然的探测时机——用户正关注库存状态，回答意愿最高；且长周期囤货信息必须先收集，后续计划才能真正感知库存。实测佐证：08-04 生成了 24 项购物清单但 pantry.json 仍为空——单一触发点导致该信息永远收不到
- 被否决的选项:
  - 维持现状（探测只在周计划 Step 1 触发）—— 否决：实测证明信息收集不到，且错过了回答意愿最高的时刻
  - 每次查看都触发且不设标记 —— 否决：反复追问违反最小打扰原则
  - 扩展到过期提醒/购买记录等所有触点 —— 否决：提醒/记账语境插入盘点询问是噪音；触发点应限于用户主动查看库存的语境
- 备注: commit 0ad1af2；`meta.longCycleProbed` 缺失视为 false，向后兼容已有数据文件

## [2026-08-04] 新增 Weekly Plan 能力模块（按周采购规划 + 每日搭配）
- 决策: 在 pantry-man skill 中新增 **Weekly Plan** 模块：用户画像驱动 → 每周 2 次采购规划（每次 3-4 天食材）→ 每日食材搭配建议；采购清单写入现有 shopping.json 形成闭环
- 理由: 真实试用后痛点验证——记录/查询/提醒是录入负担，用户要的是"规划产出价值"。规划功能复用现有数据层（购物清单/购买记录/库存），不另起炉灶，价值密度远高于被动记录
- 实施路径: 分两阶段——①画像→周规划→采购清单（不依赖库存）②规划参考库存剩余联动
- 画像形态: 可选 `profile.json`，不搞问卷；agent 对话中自然累积，首周规划后让用户二次确认/修正（二次确认优于一次问卷）
- 被否决的选项:
  - 做"画像问卷选择器"（家族基因/饮食文化/偏好一次性选择）—— 否决：首次使用变重，与 First-Run 轻体验原则冲突；且健康数据前置收集有隐私风险
  - 把"推荐食材"作为画像的落地场景 —— 否决：推荐准确性主要取决于模型知识而非粗分类画像，边际价值低；画像的正确场景是周规划
  - 另起独立 skill 做规划 —— 暂缓：闭环数据流（规划写回 shopping.json）在同一 skill 内实现最自然，独立 skill 留作未来拆分选项
- 风险与缓解: 涉及健康建议（糖尿病/冠心病史）→ SKILL.md 必须加入"非医疗建议，重大健康问题请咨询医生"免责声明
- 备注: 用户示例画像——中国人、父辈糖尿病+冠心病史、避免精制碳水/低GI碳水、少不健康油脂、加入心脑血管友好油脂、偏好蒸/煮/凉拌极简烹饪

## [2026-07-31] 优先修复首次初始化（First-Run Setup）
- 决策: 在 SKILL.md 的 Core Operations 前加入 `## First-Run Setup` 段落，要求 agent 在任何操作前检查并创建数据目录和种子文件
- 理由: 从 UX 角度，最严重的问题是 Zero-to-One 体验断裂——用户第一次交互就遇到"文件不存在"报错，信任在首次使用时崩塌
- 被否决的选项:
  - 把 `[AGENT_HOME]` 硬编码为 `~/.hermes/` —— 否决，因为 skill 目标是跨 agent 通用（Open Agent Skills 规范）
  - 只修数据路径不修初始化 —— 否决，路径问题不是最严重的 UX 问题
- 备注: commit 32c8c0f；用户明确要求不要预设 Hermes 使用
